import{_ as s,M as t,p as l,q as o,Q as i,t as e,N as n,U as d,a1 as r}from"./framework-344bb0e4.js";const c={},p=r(`<h1 id="aliases" tabindex="-1"><a class="header-anchor" href="#aliases" aria-hidden="true">#</a> Aliases</h1><p>Aliases in Nushell offer a way of doing a simple replacement of command calls (both external and internal commands). This allows you to create a shorthand name for a longer command, including its default arguments.</p><p>For example, let&#39;s create an alias called <code>ll</code> which will expand to <code>ls -l</code>.</p><div class="language-text" data-ext="text"><pre class="language-text"><code>&gt; alias ll = ls -l
</code></pre></div><p>We can now call this alias:</p><div class="language-text" data-ext="text"><pre class="language-text"><code>&gt; ll
</code></pre></div><p>Once we do, it&#39;s as if we typed <code>ls -l</code>. This also allows us to pass in flags or positional parameters. For example, we can now also write:</p><div class="language-text" data-ext="text"><pre class="language-text"><code>&gt; ll -a
</code></pre></div><p>And get the equivalent to having typed <code>ls -l -a</code>.</p><h2 id="list-all-loaded-aliases" tabindex="-1"><a class="header-anchor" href="#list-all-loaded-aliases" aria-hidden="true">#</a> List all loaded aliases</h2><p>Your useable aliases can be seen in <code>$nu.scope.aliases</code>.</p><h2 id="persisting" tabindex="-1"><a class="header-anchor" href="#persisting" aria-hidden="true">#</a> Persisting</h2><p>To make your alias persistent it must be added to your <em>config.nu</em> file.</p>`,13);function h(u,g){const a=t("RouterLink");return l(),o("div",null,[p,i("p",null,[e("For more details about how to persist aliases so that they're visible when you start up Nushell, see the "),n(a,{to:"/book/configuration.html"},{default:d(()=>[e("configuration chapter")]),_:1}),e(".")])])}const x=s(c,[["render",h],["__file","aliases.html.vue"]]);export{x as default};
